# 
pkg update && pkg upgrade -y

# Install required tools
pkg install wget unzip -y
wget https://termux.org/bootstrap/bootstrap-${ARCH}.zip -O bootstrap.zip

# Extract it to the Termux prefix
unzip -o bootstrap.zip -d $PREFIX

https://www.mediafire.com/file/vse4uaqzndxvq95/XoDos_apt-android-7-debug_arm64-v8a.apk/file￼Enter
